import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'GESTION_PEDA_IONIC',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
